<div class="bike">
  <%= link_to bike.name, 
    bike_path(bike) %>
  <div class="bike__actions">
    <%= button_to "Delete",
      bike_path(bike),
      method: :delete,
      class: "btn btn--light" %>
    <%= link_to "Edit",
      edit_bike_path(bike),
      class: "btn btn--light" %>
  </div>
</div>
